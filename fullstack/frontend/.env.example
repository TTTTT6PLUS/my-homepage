# .env.example —— 前端环境变量说明书（给未来的主人看的）
# Vite 只认 .env 文件里 VITE_ 开头的变量；此文件只是示例，不会被自动读取。
#
# 本地开发：什么都不用配 —— api.ts 里已有兜底地址 http://127.0.0.1:8001/api
# 部署到 Vercel：在面板的 Environment Variables 里新增一行（值换成 Render 后端真实公网地址）：
#   VITE_API_BASE = https://你的后端名.onrender.com/api
#
# 想要本地"模拟线上"？把本文件复制成 .env.local 再改成线上后端地址即可：
# VITE_API_BASE=http://127.0.0.1:8001/api
